{
  "name": "react-fastify-starter",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "build:server-docker": "docker compose -f server/docker-compose.dev.yml -p react-fastify-starter-dev up -d --build --force-recreate"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/johchai/react-fastify-starter.git"
  }
}
