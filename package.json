{
  "name": "react-fastify-starter",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "packages/*"
  ],
  "scripts": {
    "build:type": "cd server && openapi-ts && cd ../ && npm run build --prefix packages/types"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/johchai/react-fastify-starter.git"
  }
}
