export type BookProps = {
  data: {
    title: string;
    description: string;
  };
};

// This is a placeholder for the actual API call to fetch books
// export const GetBooks = async (): Promise<<BookProps>> => {};
